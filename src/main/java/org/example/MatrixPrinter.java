package org.example;
/**
 * Interface for printing a Matrix as a String.
 */
public interface MatrixPrinter {
    /**
     * Converts a Matrix into a formatted String.
     *
     * @param matrix the matrix to print
     * @return the formatted string
     */
    String print(Matrix matrix);
}
